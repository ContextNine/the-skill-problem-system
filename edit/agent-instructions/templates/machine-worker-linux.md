## Linux worker

User-installed fleet commands and portable skill snapshots live below the home directory. The Codex Browser plugin runtime is unavailable here. Use the installed `playwright-cli` for browser visual and interaction checks when they are part of the task.

Return worker results through {primary_display_name} (`{primary_id}`), the primary Mac using {primary_machine_access_provider} at `{primary_mesh_address}`.
