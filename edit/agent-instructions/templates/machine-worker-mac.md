## Worker Mac

User-installed fleet commands live in `~/.local/bin`. Return worker results through {primary_display_name} (`{primary_id}`), the primary Mac using {primary_machine_access_provider} at `{primary_mesh_address}`.
