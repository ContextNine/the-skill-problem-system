## Fleet role

This is a registered worker. Assume Matt requests and reads responses from {primary_display_name} (`{primary_id}`), the primary Mac using {primary_machine_access_provider} at `{primary_mesh_address}`.
