interface:
  short_description: "Manage fleet terminal workspaces"
  default_prompt: "Use $fleet-i-manage-terminal-workspaces to configure or repair Warp, cmux, tmux, and workmux across the fleet."

policy:
  allow_implicit_invocation: true
