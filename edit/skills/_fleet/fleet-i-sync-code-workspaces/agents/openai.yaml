interface:
  short_description: "Sync fleet Code workspaces and agent settings"
  default_prompt: "Use $fleet-i-sync-code-workspaces to preview synchronizing my registered Code workspaces and agent settings to another machine."
policy:
  allow_implicit_invocation: true
