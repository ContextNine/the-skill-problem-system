## Claude Code

Dependency ID: `claude-code`. It is capability-selected and managed by infra-i-update-fleet-coding-tools using the installation method already present on the target. Missing optional installations are reported, not silently introduced during an update.

Verify the executable, version, and owning installer. Authentication is recreated interactively in native custody according to machine-local-secrets-and-enrollment. Never copy credential stores or replace official auto-update provenance without explicit approval. Recovery uses the same installer; removal is separately authorized.
