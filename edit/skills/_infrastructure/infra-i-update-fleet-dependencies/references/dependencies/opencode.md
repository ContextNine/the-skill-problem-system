## OpenCode

Dependency ID: `opencode`. It is capability-selected and updated by [[infra-i-update-fleet-coding-tools]] only when already installed or explicitly selected for onboarding. Preserve the current package provenance and verify the resolved executable and version.

Provider authentication is target-local and never part of fleet distribution. Recovery reinstalls through the same approved channel. Removal does not happen during routine sync or update.
